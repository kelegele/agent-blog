---
---
<footer>
  <div class="footer-inner">
    <span>© 2019–{new Date().getFullYear()} 可了个乐</span>
    <span class="sep">·</span>
    <a href="https://github.com/kelegele/blog.kelegele.com" target="_blank" rel="noopener">Source</a>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--c-hairline);
    padding: var(--s-lg);
    text-align: center;
    font-size: 12px;
    color: var(--c-mute);
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--s-xs);
  }

  .sep {
    color: var(--c-hairline-strong);
  }

  a {
    color: var(--c-mute);
    transition: color 0.15s;
  }

  a:hover {
    color: var(--c-ink);
  }
</style>
